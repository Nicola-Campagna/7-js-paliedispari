ES-1:
* crea un array vuoto e genera un numero random che corrisponderà agli elementi che sarà composto l'array ;
* cicla per quel numero di volte uan funzione che genera numeri random tra 1 e 100 e inseriamoli nell'array ;
* dire con una funzione se i numeri dell'array sono pari o dispari ...

( BONUS )
* inserire nel DOM i numeri divisi in pari e dispari ;
* partendo dall'array principale spostare i numeri pari in un nuovo array per numeri pari e stessa cosa per numeri dispari ;